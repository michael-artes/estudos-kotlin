package operadores

fun main() {
    var a: Int = 7
    var b: Int = 3

    b += a // b = b + a
    b -= 4 // b = b - a
    b *= 2 // b = b * a
    b /= 2 // b = b / a
    b += 2 // b = b + a
    b %= 2 // b = b + a
}